// A single-line comment to explain the code

// Declaring a constant variable to store a string value
const greeting = "Hello, World!";

// Declaring a variable whose value can be changed
let count = 42;

// Outputting the value of a variable to the browser console for debugging
console.log(greeting);
